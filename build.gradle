apply plugin: 'java'

/**
 * Remove for micro-deploy
 */
apply plugin: 'application'
mainClassName

sourceCompatibility = '1.8'
targetCompatibility = '1.8'

repositories {
    mavenCentral()
}

dependencies {
    compile 'org.slf4j:slf4j-api:1.7.5'
    testCompile "junit:junit:4.11"
}